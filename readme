Вопросы 💎

1. Как создать дату 24 января 2021 года, 22 часа 51 минута? Временная зона — местная. 

console.log(new Date(2021, 0, 24, 22, 51)); 

2. Для чего предназначен метод `getDay()`?

Вернёт день недели от 0 (воскресенье) до 6 (суббота).

3. Как посчитать, сколько секунд осталось до завтра?

let tomorrow = new Date(new Date().getTime() + 24 * 60 * 60 * 1000);
tomorrow.setHours(0, 0, 0, 0);
let diff = Math.round((tomorrow - new Date())/1000);
console.log(diff + ` seconds until tomorrow`);

4. Для чего предназначен метод `getDate()`?

Для получения из объекта Date дня месяца от 1 до 31. 

5. Что выведет `console.log(d)` ?
    
    let d = new Date(2016, 2, 9); 
    console.log(d);

В консоль будет выведена заданная дата со временем: Wed Mar 09 2016 00:00:00. Поскольку время не указано, то оно ставится в полночь по Гринвичу и меняется в соответствии с часовым поясом места выполнения кода. 

6. Что делает `getTimezoneOffset()` ?

Возвращает разницу в минутах между местным часовым поясом и UTC.

7. Что выведет консоль?
    
    let date = new Date();
    console.log(date.getUTCHours());

    Консоль выведет час в часовом поясе UTC+0. 
    
8. Для чего предназначен метод `getHours()` объекта Date?

Для получения часа из объекта Date. 

9. Что выведет консоль?
    
    let d = new Date(); 
    let y =  d.getFullYear();
    console.log(y);

    Консоль выведет текущий год - 2023.
    
10. В чём ошибка в коде?
    
    let y = new Date(); 
    let d = y.getDate();
    console.log(d);

В данном коде нет ошибки.